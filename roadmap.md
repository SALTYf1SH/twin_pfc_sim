# 面向深度学习代理模型的数据集构建与训练路线图

本文档详细规划了如何基于当前的PFC模拟项目，构建一个用于训练深度学习代理模型的数据集。该代理模型旨在根据地表沉降数据和岩层属性，快速预测地下的裂隙分布情况。

**核心思路**：将PFC模拟视为一个高精度的“数字实验室”，通过批量运行多样化的模拟场景，生成大量的 `(输入, 输出)` 数据对。然后，利用这些数据训练一个计算成本低的深度学习模型（代理模型），以替代昂贵的PFC模拟，实现快速预测。

**数据格式定义**:
- **输入 (X)**: 一个一维向量，由两部分拼接而成：
    1.  **地表沉降数据**: 当前地表各个监测点的垂直位移。
    2.  **岩层属性数据**: 描述地质结构和力学特性的参数（如厚度、弹性模量、内聚力等）。
- **输出 (Y)**: 一个一维向量，代表了地下空间的裂隙密度分布。这是通过将地下空间划分为固定网格，并统计每个网格单元内的碎块面积之和得到的。

---

## 阶段一：数据生成引擎的构建与验证 (预计1-2周)

*此阶段的核心目标是改造现有项目，使其成为一个能自动化、批量生成高质量数据的引擎。*

1.  **参数化改造与配置 (`config_sd.json`)**
    *   **任务**: 审查并确定所有需要作为模型输入的岩层及开采参数。确保它们都已包含在 `config_sd.json` 中，并且易于程序化修改。
    *   **产出**: 一个最终版的 `config_sd.json` 模板，涵盖所有可变参数。

2.  **创建参数采样脚本 (`parameter_sampler.py`)**
    *   **任务**: 编写一个独立的Python脚本，用于生成多样化的参数组合。
    *   **技术选型**: 优先考虑使用**拉丁超立方采样 (Latin Hypercube Sampling)**，因为它能以较少的样本数实现对参数空间的均匀覆盖。可使用 `scipy.stats.qmc.LatinHypercube`。
    *   **产出**: 一个能输出 `N` 组参数配置（例如，保存为 `N` 个独立的 `.json` 文件或一个大的CSV文件）的脚本。

3.  **开发批量模拟执行器 (`batch_runner.py`)**
    *   **任务**: 编写一个主控脚本，该脚本能：
        a. 循环读取由 `parameter_sampler.py` 生成的参数配置。
        b. 每次循环时，用当前参数配置覆盖 `config_sd.json`。
        c. 调用PFC命令行，执行 `main3.py` 进行一次完整的模拟。
        d. 记录每次模拟的成功或失败状态，并提供断点续传功能。
    *   **产出**: 一个可以无人值守、连续执行数百次PFC模拟的自动化脚本。

4.  **开发数据提取与处理脚本 (`data_extractor.py`)**
    *   **任务**: 这是最关键的脚本。它需要能处理**单次**PFC模拟的结果文件夹，并提取出所有开采步骤的数据对。
        a. **输入 (X) 提取**:
           - 读取 `simulation_parameters.txt` 或对应的配置文件获取岩层属性。
           - 读取 `surface_y_disp_vs_section.csv`，按列提取每个开采步骤的地表沉降向量。
        b. **输出 (Y) 生成 (核心)**:
           - 循环读取每个 `fragments_properties_step_*.csv` 文件。
           - **实现**: 将地下空间划分为一个固定的网格（例如 `64x64`）。遍历所有碎块，将每个碎块的面积 (`area`) 累加到其质心 (`centroid_x`, `centroid_y`) 所在的网格单元中。最后，将这个二维网格展平 (flatten) 为一个一维向量。
        c. **数据配对与存储**:
           - 将提取的输入向量 (X) 和生成的输出向量 (Y) 以统一的命名规则保存。
           - **推荐格式**: 使用 NumPy 的 `.npz` 格式，将X和Y两个数组压缩存储在同一个文件中，例如 `np.savez_compressed('scene_XXX_step_YY.npz', x=input_vector, y=output_vector)`。
    *   **产出**: 一个能将PFC原始输出转化为配对好的 `(X_vector, Y_vector)` 样本的脚本。

5.  **端到端测试**
    *   **任务**: 生成一个小的参数集（例如5-10组），完整地运行整个数据生成流程，手动检查生成的数据集是否正确、完整、无误。
    *   **产出**: 验证整个数据生成流程的正确性，并修复潜在的bug。

---

## 阶段二：大规模数据生成 (预计1-3周，取决于计算资源)

*此阶段是计算密集型的，目标是生成足够数量和多样性的数据。*

1.  **执行批量模拟**
    *   **任务**: 在服务器或高性能计算机上，运行 `batch_runner.py`，生成数百到数千次模拟。
    *   **监控**: 定期检查模拟进度和成功率。
    *   **产出**: 包含所有原始模拟结果的庞大文件夹。

2.  **执行数据提取**
    *   **任务**: 运行 `data_extractor.py`，处理所有模拟结果，生成最终的、可供训练使用的数据集。
    *   **产出**: 一个包含所有 `.npz` 数据对的最终数据集文件夹。

---

## 阶段三：深度学习模型的设计与训练 (预计2-4周)

*此阶段进入机器学习领域，目标是训练出性能优良的代理模型。*

1.  **模型选型与设计**
    *   **任务**: 核心任务是“向量到向量”的回归预测。
    *   **推荐模型**: **全连接神经网络 (Fully Connected Neural Network, FNN)**，也叫**多层感知机 (Multi-Layer Perceptron, MLP)**。
    *   **建议结构**:
        *   **输入层**: 神经元数量等于输入向量 (X) 的维度。
        *   **隐藏层**: 包含2到5个隐藏层，使用ReLU等激活函数。
        *   **输出层**: 神经元数量等于输出向量 (Y) 的维度，不使用激活函数。

2.  **数据加载与预处理 (`dataset.py`, `train.py`)**
    *   **任务**: 编写PyTorch或TensorFlow的数据加载器。
        a. 从 `.npz` 文件中高效地读取输入 (X) 和输出 (Y) 向量。
        b. **关键**: 对输入和输出向量进行归一化 (Normalization) 或标准化 (Standardization)。
        c. 将数据划分为训练集、验证集和测试集。
    *   **产出**: 高效的数据加载管道。

3.  **模型训练与调优 (`train.py`)**
    *   **任务**:
        a. 编写训练循环，包括前向传播、损失计算、反向传播和优化器更新。
        b. **损失函数**: 使用 **均方误差 (Mean Squared Error, MSE)** 或 **平均绝对误差 (Mean Absolute Error, MAE)**。
        c. 使用TensorBoard或WandB等工具监控训练过程。
        d. 调整学习率、批大小 (batch size)、网络深度等超参数。
    *   **产出**: 一个或多个训练好的模型权重文件。

4.  **模型评估 (`evaluate.py`)**
    *   **任务**: 在测试集上评估模型的性能。
        a. **定性评估**: 将模型预测的输出向量 (Y_pred) 和真实的输出向量 (Y_true) **重新绘制成二维的裂隙密度图**，进行并排比较。
        b. **定量评估**:
           - 在向量上直接计算评估指标，如 **MSE**、**MAE** 或 **R² (决定系数)**。
           - 在重新绘制的图像上计算 **结构相似性指数 (SSIM)**，以评估空间分布的相似性。
    *   **产出**: 对模型性能的最终评估报告。

---

## 阶段四：模型部署与应用 (可选)

*将训练好的模型集成到一个简单的工具中，实现快速预测。*

1.  **创建预测接口 (`predictor.py`)**
    *   **任务**: 编写一个脚本，该脚本可以加载训练好的模型权重，接收一个新的输入，并快速输出预测的裂隙分布向量。
2.  **构建简单的UI (可选)**
    *   **任务**: 使用Gradio、Streamlit或简单的GUI库，为预测器创建一个用户友好的界面，实现结果的可视化。
